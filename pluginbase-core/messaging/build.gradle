plugins {
    id 'com.github.johnrengelman.shadow' version '2.0.1'
}

description = ''
dependencies {
    shadow project(':PluginBase-Core:Logging')
    shadow group: 'ninja.leaping.configurate', name: 'configurate-core', version: configurateVersion
    compile project(':PluginBase-Core:Messages')
    testCompile group: 'ninja.leaping.configurate', name: 'configurate-hocon', version: configurateVersion
}

shadowJar {
    baseName = project.name
    classifier = null
    exclude 'tools.jar'
}
